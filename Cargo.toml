[package]
name = "timeRs"
version = "0.2.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.53", default-features = false, features = ["derive", "std"] }
num_cpus =  "1.17.0"

[profile.release]
strip = true
lto = "thin"
panic = "abort"
opt-level = "z"
debug = false
codegen-units = 1

